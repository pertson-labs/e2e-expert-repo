PREFIX narr: <https://aswritten.ai/narrative#>
PREFIX rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX dct:  <http://purl.org/dc/terms/>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX oa:   <http://www.w3.org/ns/oa#>

# Transaction provenance (default graph)
INSERT DATA {
  narr:Tx_20260707T140208Z a prov:Activity ;
    prov:generatedAtTime "2026-07-07T14:02:08Z"^^xsd:dateTime ;
    prov:wasAssociatedWith "aswritten.ai" ;
    prov:wasAttributedTo "n8n.aswritten.ai" ;
    narr:originPath ".aswritten/memories/2026-07-07-1-sample-memory-distinctive-heading-slug.md" ;
    narr:originRef "e2e-derive-id-1783432778927" ;
    dct:title "Sample Memory With Distinctive Heading — slug-gen test" ;
    prov:generated
      narr:Sample_SampleMemoryDistinctiveHeading,
      narr:Narrative_PipelineSlugGeneration,
      narr:Claim_PipelineMustProduceSlug,
      narr:Actor_XenaStaging,
      narr:StyleObs_DirectDeclarativeTone,
      narr:StyleObs_TechnicalJargonPipeline,
      narr:StyleObs_EmDashCadence,
      narr:StyleMetrics_SampleMemoryDistinctiveHeading .
} ;

# Extracted entities (named graph)
INSERT DATA {
  GRAPH narr:Tx_20260707T140208Z {

    # ── Sample ──────────────────────────────────────────────────
    narr:Sample_SampleMemoryDistinctiveHeading a narr:Sample ;
      dct:title "Sample Memory With Distinctive Heading" ;
      narr:inputLength 295 ;
      dct:description "Test memory submitted without an explicit id field to exercise the pipeline slug-generation path (LLM id-gen or deterministic slugify fallback)." ;
      dct:created "2025-07-15"^^xsd:date .

    # ── Actor ───────────────────────────────────────────────────
    narr:Actor_XenaStaging a narr:Actor ;
      skos:prefLabel "xena+staging@aswritten.ai" ;
      skos:note "Reviewer listed in the YAML front-matter of the test memory submission." .

    # ── Narrative ───────────────────────────────────────────────
    narr:Narrative_PipelineSlugGeneration a narr:Narrative ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      narr:theme "Pipeline slug generation from memory content when id field is absent" ;
      skos:broader narr:Process ;
      skos:note "When a memory is submitted without an explicit id field, the pipeline must derive a slug — either via an LLM id-gen call or a deterministic slugify fallback — and commit the file successfully. This narrative captures the expected pipeline behaviour as a test fixture." .

    # ── Claim ───────────────────────────────────────────────────
    narr:Claim_PipelineMustProduceSlug a narr:Claim ;
      narr:aboutNode narr:Narrative_PipelineSlugGeneration ;
      narr:hasConvictionLevel narr:Conviction_Notion ;
      narr:supports narr:Process ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "The pipeline must produce a slug from the memory content — either via the LLM id-gen call or the deterministic slugify fallback — and commit the file successfully." .

    # ── Style Observations ──────────────────────────────────────

    # 1. Direct declarative tone
    narr:StyleObs_DirectDeclarativeTone a narr:StyleObservation ;
      skos:broader narr:ToneAuthoritative ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "Imperative declarative register: 'The pipeline must produce a slug' — no hedging, no conditionals." ;
      rdf:value true ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 92 ; oa:end 159
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "Submitted without an explicit id field. The pipeline must produce a" ;
          oa:prefix "emory With Distinctive Heading\n\n" ;
          oa:suffix "\nslug from the memory content — "
        ]
      ] .

    # 2. Technical jargon density (pipeline/slug/LLM/fallback)
    narr:StyleObs_TechnicalJargonPipeline a narr:StyleObservation ;
      skos:broader narr:JargonPolicy ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "High jargon density for a short text: 'pipeline', 'LLM id-gen call', 'deterministic slugify fallback' — assumes a technical reader audience." ;
      rdf:value true ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 160 ;
          oa:end 295
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "slug from the memory content — either via the LLM id-gen call or the\ndeterministic slugify fallback — and commit the file successfully." ;
          oa:prefix "ld. The pipeline must produce a\n" ;
          oa:suffix ""
        ]
      ] .

    # 3. Em-dash cadence for parenthetical alternatives
    narr:StyleObs_EmDashCadence a narr:StyleObservation ;
      skos:broader narr:CadenceRhythm ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "Em-dash used twice to bracket an inline alternative ('— either via … or the … —'), creating a parenthetical rhythm that keeps the main clause intact." ;
      rdf:value true ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 160 ;
          oa:end 295
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "slug from the memory content — either via the LLM id-gen call or the\ndeterministic slugify fallback — and commit the file successfully." ;
          oa:prefix "ld. The pipeline must produce a\n" ;
          oa:suffix ""
        ]
      ] .

    # ── Style Metrics ───────────────────────────────────────────
    narr:StyleMetrics_SampleMemoryDistinctiveHeading a narr:StyleMetrics ;
      skos:broader narr:StyleMetrics ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      narr:AverageSentenceLength 22 ;
      narr:ActiveVoiceRatio "0.9"^^xsd:decimal ;
      narr:JargonDensity "0.18"^^xsd:decimal ;
      skos:note "Short document (2 sentences in body). Active voice dominant ('The pipeline must produce', 'commit the file'). Technical jargon concentrated in the second sentence (pipeline, LLM, slugify, fallback)." .

  }
}