PREFIX narr: <https://aswritten.ai/narrative#>
PREFIX rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX dct:  <http://purl.org/dc/terms/>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX oa:   <http://www.w3.org/ns/oa#>

# Transaction provenance (default graph)
INSERT DATA {
  narr:Tx_20260704T103849Z a prov:Activity ;
    prov:generatedAtTime "2026-07-04T10:38:49Z"^^xsd:dateTime ;
    prov:wasAssociatedWith "aswritten.ai" ;
    prov:wasAttributedTo "n8n.aswritten.ai" ;
    narr:originPath ".aswritten/memories/2026-07-04-1-sample-memory-slug-generation-test.md" ;
    narr:originRef "e2e-derive-id-1783161385522" ;
    dct:title "Sample Memory With Distinctive Heading — slug derivation test" ;
    prov:generated
      narr:Sample_SampleMemoryDistinctiveHeading,
      narr:Narrative_PipelineSlugDerivation,
      narr:Claim_PipelineMustProduceSlug,
      narr:StyleObs_TechnicalDirectRegister,
      narr:StyleObs_ImperativeMood,
      narr:StyleObs_PipelineJargon,
      narr:StyleMetrics_SampleMemoryDistinctiveHeading .
} ;

# Extracted entities (named graph)
INSERT DATA {
  GRAPH narr:Tx_20260704T103849Z {

    # ── Sample ──────────────────────────────────────────────────
    narr:Sample_SampleMemoryDistinctiveHeading a narr:Sample ;
      dct:title "Sample Memory With Distinctive Heading" ;
      narr:inputLength 295 ;
      dct:description "Test memory submitted without an explicit id field to exercise the pipeline slug-generation path (LLM id-gen or deterministic slugify fallback)." .

    # ── Narrative ───────────────────────────────────────────────
    narr:Narrative_PipelineSlugDerivation a narr:Narrative ;
      narr:theme "Pipeline slug derivation from memory content" ;
      skos:broader narr:Process ;
      skos:note "When a memory is submitted without an explicit id field, the pipeline must derive a slug — either via an LLM id-gen call or a deterministic slugify fallback — and commit the file successfully. This memory exists to exercise and validate that path." ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading .

    # ── Claim ───────────────────────────────────────────────────
    narr:Claim_PipelineMustProduceSlug a narr:Claim ;
      narr:aboutNode narr:Narrative_PipelineSlugDerivation ;
      narr:hasConvictionLevel narr:Conviction_Boulder ;
      narr:supports narr:Process ;
      skos:note "The pipeline must produce a slug from the memory content — either via the LLM id-gen call or the deterministic slugify fallback — and commit the file successfully." ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading .

    # ── Style Observations ──────────────────────────────────────

    # 1. Technical-direct register
    narr:StyleObs_TechnicalDirectRegister a narr:StyleObservation ;
      skos:broader narr:ToneAuthoritative ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "Declarative, specification-style prose: states what the pipeline 'must' do without hedging." ;
      rdf:value true ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 132 ;
          oa:end 188
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "The pipeline must produce a\nslug from the memory content" ;
          oa:prefix "d without an explicit id field. " ;
          oa:suffix " — either via the LLM id-gen cal"
        ]
      ] .

    # 2. Imperative mood / obligation framing
    narr:StyleObs_ImperativeMood a narr:StyleObservation ;
      skos:broader narr:SentenceMood ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "Use of modal 'must' signals a hard requirement rather than a suggestion, consistent with engineering specification style." ;
      rdf:value true ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 145 ; oa:end 157
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "must produce" ;
          oa:prefix "explicit id field. The pipeline " ;
          oa:suffix " a\nslug from the memory content "
        ]
      ] .

    # 3. Pipeline / engineering jargon density
    narr:StyleObs_PipelineJargon a narr:StyleObservation ;
      skos:broader narr:JargonPolicy ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "Concentrated use of technical terms: 'pipeline', 'slug', 'LLM id-gen call', 'deterministic slugify fallback', 'commit'. Assumes an engineering audience with no lay glossing." ;
      rdf:value true ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 191 ;
          oa:end 295
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "either via the LLM id-gen call or the\ndeterministic slugify fallback — and commit the file successfully." ;
          oa:prefix "\nslug from the memory content — " ;
          oa:suffix ""
        ]
      ] .

    # ── Style Metrics ───────────────────────────────────────────
    narr:StyleMetrics_SampleMemoryDistinctiveHeading a narr:StyleMetrics ;
      skos:broader narr:StyleMetrics ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      narr:AverageSentenceLength 22 ;
      narr:ActiveVoiceRatio "0.85"^^xsd:decimal ;
      narr:JargonDensity "0.18"^^xsd:decimal ;
      skos:note "Short document (2 sentences in body). Active-voice dominant; high jargon density for its length due to technical terms (pipeline, slug, LLM, slugify, commit). Formal engineering register." .

  }
}