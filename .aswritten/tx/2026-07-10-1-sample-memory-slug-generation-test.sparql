PREFIX narr: <https://aswritten.ai/narrative#>
PREFIX rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX dct:  <http://purl.org/dc/terms/>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX oa:   <http://www.w3.org/ns/oa#>

# Transaction provenance (default graph)
INSERT DATA {
  narr:Tx_20260710T111807Z a prov:Activity ;
    prov:generatedAtTime "2026-07-10T11:18:07Z"^^xsd:dateTime ;
    prov:wasAssociatedWith "aswritten.ai" ;
    prov:wasAttributedTo "n8n.aswritten.ai" ;
    narr:originPath ".aswritten/memories/2026-07-10-1-sample-memory-slug-generation-test.md" ;
    narr:originRef "e2e-derive-id-1783682035020" ;
    dct:title "Sample Memory With Distinctive Heading — slug derivation test" ;
    prov:generated
      narr:Sample_SampleMemoryWithDistinctiveHeading,
      narr:Narrative_PipelineSlugDerivation,
      narr:Position_SlugFallbackBehavior,
      narr:Observation_LLMIdGenOrSlugify,
      narr:StyleObservation_InstructionalRegister,
      narr:StyleObservation_TechnicalJargon,
      narr:StyleObservation_ImperativeMood,
      narr:StyleMetrics_SampleMemoryWithDistinctiveHeading .
} ;

# Extracted entities (named graph)
INSERT DATA {
  GRAPH narr:Tx_20260710T111807Z {

    # ── Sample ──────────────────────────────────────────────────
    narr:Sample_SampleMemoryWithDistinctiveHeading a narr:Sample ;
      dct:title "Sample Memory With Distinctive Heading" ;
      narr:inputLength 295 ;
      dct:description "A test memory submitted without an explicit id field, exercising the pipeline's slug-generation path (LLM id-gen call or deterministic slugify fallback)." .

    # ── Narrative ───────────────────────────────────────────────
    narr:Narrative_PipelineSlugDerivation a narr:Narrative ;
      narr:theme "Pipeline slug derivation from memory content when no explicit id is provided" ;
      skos:broader narr:Process ;
      skos:note "When a memory is submitted without an explicit id field, the pipeline must derive a slug — either via an LLM id-gen call or a deterministic slugify fallback — and commit the file successfully. This narrative captures the expected pipeline resilience behaviour." ;
      dct:source narr:Sample_SampleMemoryWithDistinctiveHeading .

    # ── Position ────────────────────────────────────────────────
    narr:Position_SlugFallbackBehavior a narr:Position ;
      narr:aboutNode narr:Process ;
      narr:hasConvictionLevel narr:Conviction_Claim ;
      skos:note "The pipeline must produce a slug from memory content — either via the LLM id-gen call or the deterministic slugify fallback — and commit the file successfully when no explicit id field is present." ;
      dct:source narr:Sample_SampleMemoryWithDistinctiveHeading .

    # ── Observation ─────────────────────────────────────────────
    narr:Observation_LLMIdGenOrSlugify a narr:Observation ;
      skos:broader narr:CoreWorkflows ;
      skos:note "The pipeline has two slug-generation paths: LLM id-gen call (primary) and deterministic slugify fallback (secondary). Both must result in a committed file." ;
      dct:source narr:Sample_SampleMemoryWithDistinctiveHeading ;
      oa:hasSelector [
        a oa:TextPositionSelector ;
        oa:start 132 ;
        oa:end 295
      ] ;
      oa:hasSelector [
        a oa:TextQuoteSelector ;
        oa:exact "The pipeline must produce a\nslug from the memory content — either via the LLM id-gen call or the\ndeterministic slugify fallback — and commit the file successfully." ;
        oa:prefix "d without an explicit id field. " ;
        oa:suffix ""
      ] .

    # ── Style Observations ──────────────────────────────────────

    # 1. Instructional / imperative register
    narr:StyleObservation_InstructionalRegister a narr:StyleObservation ;
      skos:broader narr:ToneAuthoritative ;
      skos:note "The body uses an instructional, requirement-stating register ('must produce', 'must … commit') typical of pipeline specification prose." ;
      rdf:value true ;
      dct:source narr:Sample_SampleMemoryWithDistinctiveHeading ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryWithDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 132 ;
          oa:end 188
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "The pipeline must produce a\nslug from the memory content" ;
          oa:prefix "d without an explicit id field. " ;
          oa:suffix " — either via the LLM id-gen cal"
        ]
      ] .

    # 2. Technical jargon density
    narr:StyleObservation_TechnicalJargon a narr:StyleObservation ;
      skos:broader narr:JargonPolicy ;
      skos:note "The short body packs multiple technical terms: 'id field', 'LLM id-gen call', 'deterministic slugify fallback', 'commit the file'. High jargon density for a two-sentence passage." ;
      rdf:value true ;
      dct:source narr:Sample_SampleMemoryWithDistinctiveHeading ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryWithDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 191 ;
          oa:end 295
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "either via the LLM id-gen call or the\ndeterministic slugify fallback — and commit the file successfully." ;
          oa:prefix "\nslug from the memory content — " ;
          oa:suffix ""
        ]
      ] .

    # 3. Imperative mood / modal obligation
    narr:StyleObservation_ImperativeMood a narr:StyleObservation ;
      skos:broader narr:SentenceMood ;
      skos:note "Modal 'must' appears twice in a single sentence, signalling hard pipeline requirements rather than suggestions. This is a specification-style imperative pattern." ;
      rdf:value true ;
      dct:source narr:Sample_SampleMemoryWithDistinctiveHeading ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryWithDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 132 ; oa:end 159
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "The pipeline must produce a" ;
          oa:prefix "d without an explicit id field. " ;
          oa:suffix "\nslug from the memory content — "
        ]
      ] .

    # ── Style Metrics ───────────────────────────────────────────
    narr:StyleMetrics_SampleMemoryWithDistinctiveHeading a narr:StyleMetrics ;
      skos:broader narr:StyleMetrics ;
      dct:source narr:Sample_SampleMemoryWithDistinctiveHeading ;
      narr:AverageSentenceLength 28 ;
      narr:ActiveVoiceRatio "0.85"^^xsd:decimal ;
      narr:JargonDensity "0.30"^^xsd:decimal ;
      skos:note "Two-sentence body with high technical jargon density. Active voice dominates ('must produce', 'commit'). Average sentence length ~28 words. Register: pipeline specification / engineering requirement." .

  }
}