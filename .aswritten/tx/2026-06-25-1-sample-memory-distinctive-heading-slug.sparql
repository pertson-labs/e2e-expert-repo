PREFIX narr: <https://aswritten.ai/narrative#>
PREFIX rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX dct:  <http://purl.org/dc/terms/>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX oa:   <http://www.w3.org/ns/oa#>

# Transaction provenance (default graph)
INSERT DATA {
  narr:Tx_20250115T120000Z a prov:Activity ;
    prov:generatedAtTime "2025-01-15T12:00:00Z"^^xsd:dateTime ;
    prov:wasAssociatedWith "aswritten.ai" ;
    prov:wasAttributedTo "n8n.aswritten.ai" ;
    narr:originPath "(no id field in input — slug derived from heading: sample-memory-with-distinctive-heading)" ;
    narr:originRef "main@unknown" ;
    dct:title "Sample Memory With Distinctive Heading — Pipeline Slug Generation" ;
    prov:generated
      narr:Sample_SampleMemoryDistinctiveHeading,
      narr:Actor_TestExpertReviewer,
      narr:Narrative_PipelineSlugGeneration,
      narr:Claim_PipelineMustProduceSlug,
      narr:Observation_SlugGenerationMechanism,
      narr:StyleObs_ImperativePipelineTone,
      narr:StyleObs_TechnicalJargonLLMSlugify,
      narr:StyleObs_CompoundNounStacking,
      narr:StyleMetrics_SampleMemoryDistinctiveHeading .
} ;

# Extracted entities (named graph)
INSERT DATA {
  GRAPH narr:Tx_20250115T120000Z {

    # ── Sample ──────────────────────────────────────────────────
    narr:Sample_SampleMemoryDistinctiveHeading a narr:Sample ;
      dct:title "Sample Memory With Distinctive Heading" ;
      narr:inputLength 337 ;
      dct:description "Test memory submitted without an explicit id field to validate pipeline slug generation via LLM id-gen call or deterministic slugify fallback." .

    # ── Actor ───────────────────────────────────────────────────
    narr:Actor_TestExpertReviewer a narr:Actor ;
      skos:prefLabel "test+expert-gh" ;
      skos:note "Reviewer listed in YAML frontmatter as test+expert-gh@aswritten.ai; role is expert reviewer in the aswritten.ai pipeline." .

    # ── Narrative ───────────────────────────────────────────────
    narr:Narrative_PipelineSlugGeneration a narr:Narrative ;
      narr:theme "Pipeline slug generation from memory content when no explicit id field is provided" ;
      skos:broader narr:Process ;
      skos:note "When a memory is submitted without an explicit id field, the pipeline must derive a slug — either via an LLM id-gen call or a deterministic slugify fallback — and commit the file successfully. This validates the robustness of the intake and file-naming process." ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading .

    # ── Claim ───────────────────────────────────────────────────
    narr:Claim_PipelineMustProduceSlug a narr:Claim ;
      narr:aboutNode narr:Process ;
      narr:hasConvictionLevel narr:Conviction_Stake ;
      narr:supports narr:Narrative_PipelineSlugGeneration ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "The pipeline must produce a slug from memory content when no id field is present, using either the LLM id-gen call or the deterministic slugify fallback, and must commit the file successfully." .

    # ── Observation ─────────────────────────────────────────────
    narr:Observation_SlugGenerationMechanism a narr:Observation ;
      skos:broader narr:IntakePrioritization ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "Two fallback mechanisms described for slug generation: LLM id-gen call (primary) and deterministic slugify fallback (secondary)." ;
      oa:hasSelector [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 175 ;
          oa:end 310
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "either via the LLM id-gen call or the deterministic slugify fallback — and commit the file successfully" ;
          oa:prefix "slug from the memory content — " ;
          oa:suffix "."
        ]
      ] .

    # ── Style Observations ──────────────────────────────────────

    # 1. Imperative/procedural tone
    narr:StyleObs_ImperativePipelineTone a narr:StyleObservation ;
      skos:broader narr:ToneAuthoritative ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "Imperative modal 'must' signals a hard requirement, establishing an authoritative procedural tone typical of pipeline specification documents." ;
      rdf:value true ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 130 ;
          oa:end 175
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "The pipeline must produce a slug from the memory content" ;
          oa:prefix "without an explicit id field. " ;
          oa:suffix " — either via"
        ]
      ] .

    # 2. Technical jargon density
    narr:StyleObs_TechnicalJargonLLMSlugify a narr:StyleObservation ;
      skos:broader narr:JargonPolicy ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "Compound technical terms 'LLM id-gen call' and 'deterministic slugify fallback' are used without definition, assuming a technically literate audience familiar with pipeline internals." ;
      rdf:value true ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 193 ;
          oa:end 280
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "LLM id-gen call or the deterministic slugify fallback" ;
          oa:prefix "either via the " ;
          oa:suffix " — and commit"
        ]
      ] .

    # 3. Compound noun stacking / NamingConventions
    narr:StyleObs_CompoundNounStacking a narr:StyleObservation ;
      skos:broader narr:NamingConventions ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "Three-word compound noun 'deterministic slugify fallback' stacks adjective, verb-derived noun, and noun without hyphens, a pattern common in engineering documentation for naming pipeline sub-processes." ;
      rdf:value true ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 234 ;
          oa:end 265
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "deterministic slugify fallback" ;
          oa:prefix "or the " ;
          oa:suffix " — and commit"
        ]
      ] .

    # ── Style Metrics ───────────────────────────────────────────
    narr:StyleMetrics_SampleMemoryDistinctiveHeading a narr:StyleMetrics ;
      skos:broader narr:StyleMetrics ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      narr:AverageSentenceLength 28 ;
      narr:ActiveVoiceRatio "0.75"^^xsd:decimal ;
      narr:JargonDensity "0.18"^^xsd:decimal ;
      skos:note "Short document (2 sentences in body). Active voice dominates ('The pipeline must produce', 'commit the file'). Jargon density elevated by technical pipeline terms (LLM, id-gen, slugify, fallback). Register is technical-procedural." .

  }
}