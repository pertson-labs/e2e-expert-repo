PREFIX narr: <https://aswritten.ai/narrative#>
PREFIX rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX dct:  <http://purl.org/dc/terms/>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX oa:   <http://www.w3.org/ns/oa#>

# Transaction provenance (default graph)
INSERT DATA {
  narr:Tx_20260703T211958Z a prov:Activity ;
    prov:generatedAtTime "2026-07-03T21:19:58Z"^^xsd:dateTime ;
    prov:wasAssociatedWith "aswritten.ai" ;
    prov:wasAttributedTo "n8n.aswritten.ai" ;
    narr:originPath ".aswritten/memories/2026-07-03-1-sample-memory-slug-generation-test.md" ;
    narr:originRef "e2e-derive-id-1783113455790" ;
    dct:title "Sample Memory With Distinctive Heading — slug derivation test" ;
    prov:generated
      narr:Sample_SampleMemoryWithDistinctiveHeading,
      narr:Narrative_PipelineSlugDerivation,
      narr:Claim_SlugFallbackRequired,
      narr:Observation_SlugGenDescription,
      narr:Actor_XenaStaging,
      narr:StyleObs_InstructionalRegister,
      narr:StyleObs_PassiveConstruction,
      narr:StyleObs_TechnicalJargon,
      narr:StyleMetrics_SampleMemoryWithDistinctiveHeading .
} ;

# Extracted entities (named graph)
INSERT DATA {
  GRAPH narr:Tx_20260703T211958Z {

    # ── Sample ──────────────────────────────────────────────────
    narr:Sample_SampleMemoryWithDistinctiveHeading a narr:Sample ;
      dct:title "Sample Memory With Distinctive Heading" ;
      narr:inputLength 295 ;
      dct:description "A test memory submitted without an explicit id field, exercising the pipeline's slug-derivation path (LLM id-gen or deterministic slugify fallback)." .

    # ── Narrative ───────────────────────────────────────────────
    narr:Narrative_PipelineSlugDerivation a narr:Narrative ;
      narr:theme "Pipeline slug derivation from memory content when no explicit id is provided" ;
      skos:broader narr:Process ;
      skos:note "When a memory is submitted without an explicit id field, the pipeline must derive a slug — either via an LLM id-gen call or a deterministic slugify fallback — and commit the file successfully. This memory is a test artifact for that behavior." ;
      dct:source narr:Sample_SampleMemoryWithDistinctiveHeading .

    # ── Claim ───────────────────────────────────────────────────
    narr:Claim_SlugFallbackRequired a narr:Claim ;
      narr:aboutNode narr:Process ;
      narr:hasConvictionLevel narr:Conviction_Stake ;
      narr:supports narr:Narrative_PipelineSlugDerivation ;
      dct:source narr:Sample_SampleMemoryWithDistinctiveHeading ;
      skos:note "The pipeline must produce a slug from memory content — either via the LLM id-gen call or the deterministic slugify fallback — and commit the file successfully when no explicit id field is present." .

    # ── Observation ─────────────────────────────────────────────
    narr:Observation_SlugGenDescription a narr:Observation ;
      skos:broader narr:CoreWorkflows ;
      dct:source narr:Sample_SampleMemoryWithDistinctiveHeading ;
      skos:note "Core assertion about slug derivation pipeline behavior" ;
      oa:hasSelector [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryWithDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 132 ;
          oa:end 295
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "The pipeline must produce a\nslug from the memory content — either via the LLM id-gen call or the\ndeterministic slugify fallback — and commit the file successfully." ;
          oa:prefix "d without an explicit id field. " ;
          oa:suffix ""
        ]
      ] .

    # ── Actor ───────────────────────────────────────────────────
    narr:Actor_XenaStaging a narr:Actor ;
      skos:prefLabel "xena+staging" ;
      skos:note "Reviewer listed in YAML frontmatter of the test memory. Email redacted per PII policy." .

    # ── Style Observations ──────────────────────────────────────

    # 1. Instructional / procedural register
    narr:StyleObs_InstructionalRegister a narr:StyleObservation ;
      skos:broader narr:ToneAuthoritative ;
      dct:source narr:Sample_SampleMemoryWithDistinctiveHeading ;
      skos:note "Imperative-modal construction ('must produce', 'must … commit') signals an instructional, requirements-specification register." ;
      rdf:value true ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryWithDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 132 ;
          oa:end 188
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "The pipeline must produce a\nslug from the memory content" ;
          oa:prefix "d without an explicit id field. " ;
          oa:suffix " — either via the LLM id-gen cal"
        ]
      ] .

    # 2. Passive / nominalized construction
    narr:StyleObs_PassiveConstruction a narr:StyleObservation ;
      skos:broader narr:VoicePassive ;
      dct:source narr:Sample_SampleMemoryWithDistinctiveHeading ;
      skos:note "'Submitted without an explicit id field' opens with a passive/elliptical construction, omitting the agent (the submitter), typical of technical specification prose." ;
      rdf:value true ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryWithDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 92 ; oa:end 131
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "Submitted without an explicit id field." ;
          oa:prefix "emory With Distinctive Heading\n\n" ;
          oa:suffix " The pipeline must produce a\nslu"
        ]
      ] .

    # 3. Technical jargon density
    narr:StyleObs_TechnicalJargon a narr:StyleObservation ;
      skos:broader narr:JargonPolicy ;
      dct:source narr:Sample_SampleMemoryWithDistinctiveHeading ;
      skos:note "Dense use of pipeline-specific jargon: 'slug', 'LLM id-gen call', 'deterministic slugify fallback', 'commit the file'. High jargon density for a short document." ;
      rdf:value true ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryWithDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 191 ;
          oa:end 295
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "either via the LLM id-gen call or the\ndeterministic slugify fallback — and commit the file successfully." ;
          oa:prefix "\nslug from the memory content — " ;
          oa:suffix ""
        ]
      ] .

    # ── Style Metrics ───────────────────────────────────────────
    narr:StyleMetrics_SampleMemoryWithDistinctiveHeading a narr:StyleMetrics ;
      skos:broader narr:StyleMetrics ;
      dct:source narr:Sample_SampleMemoryWithDistinctiveHeading ;
      narr:AverageSentenceLength 28 ;
      narr:ActiveVoiceRatio "0.50"^^xsd:decimal ;
      narr:JargonDensity "0.18"^^xsd:decimal ;
      skos:note "Short document (2 sentences in body). Mixed active/passive voice. High jargon density relative to word count due to technical pipeline terminology." .

  }
}