PREFIX narr: <https://aswritten.ai/narrative#>
PREFIX rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX dct:  <http://purl.org/dc/terms/>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX oa:   <http://www.w3.org/ns/oa#>

INSERT DATA {
  narr:Tx_20260705T104136Z a prov:Activity ;
    prov:generatedAtTime "2026-07-05T10:41:36Z"^^xsd:dateTime ;
    prov:wasAssociatedWith "aswritten.ai" ;
    prov:wasAttributedTo "n8n.aswritten.ai" ;
    narr:originPath ".aswritten/memories/2026-07-05-1-sample-memory-distinctive-heading-slug.md" ;
    narr:originRef "e2e-derive-id-1783247978539" ;
    dct:title "Sample Memory With Distinctive Heading — Pipeline Slug Generation" ;
    prov:generated
      narr:Sample_SampleMemoryDistinctiveHeading,
      narr:Narrative_PipelineSlugGeneration,
      narr:Claim_SlugFallbackBehavior,
      narr:Actor_XenaStagingReviewer,
      narr:StyleObs_TechnicalRegister,
      narr:StyleObs_ImperativeMood,
      narr:StyleObs_PassiveConstruction,
      narr:StyleMetrics_SampleMemoryDistinctiveHeading .
} ;

INSERT DATA {
  GRAPH narr:Tx_20260705T104136Z {

    # ── Sample ────────────────────────────────────────────────────
    narr:Sample_SampleMemoryDistinctiveHeading a narr:Sample ;
      dct:title "Sample Memory With Distinctive Heading" ;
      narr:inputLength "295"^^xsd:integer ;
      dct:description "Test memory submitted without an explicit id field to exercise pipeline slug generation via LLM id-gen or deterministic slugify fallback." .

    # ── Actor ─────────────────────────────────────────────────────
    narr:Actor_XenaStagingReviewer a narr:Actor ;
      skos:prefLabel "xena (staging reviewer)" ;
      skos:note "Reviewer listed in YAML front matter as xena+staging@aswritten.ai; staging environment actor for pipeline review." .

    # ── Narrative ─────────────────────────────────────────────────
    narr:Narrative_PipelineSlugGeneration a narr:Narrative ;
      narr:theme "Pipeline must derive a memory slug when no explicit id field is provided" ;
      skos:broader narr:CoreWorkflows ;
      skos:note "When a memory is submitted without an explicit id field, the pipeline must produce a slug from the memory content — either via the LLM id-gen call or the deterministic slugify fallback — and commit the file successfully." ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading .

    # ── Claim ─────────────────────────────────────────────────────
    narr:Claim_SlugFallbackBehavior a narr:Claim ;
      narr:aboutNode narr:CoreWorkflows ;
      narr:hasConvictionLevel narr:Conviction_Stake ;
      narr:supports narr:Narrative_PipelineSlugGeneration ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "The pipeline must produce a slug from the memory content — either via the LLM id-gen call or the deterministic slugify fallback — and commit the file successfully." .

    # ── Style Observation 1: Technical/procedural register ────────
    narr:StyleObs_TechnicalRegister a narr:StyleObservation ;
      skos:broader narr:RegisterFormality ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "Technical procedural register: pipeline mechanics described with system-level precision (LLM id-gen call, deterministic slugify fallback)." ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start "191"^^xsd:integer ;
          oa:end "259"^^xsd:integer
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "either via the LLM id-gen call or the\ndeterministic slugify fallback" ;
          oa:prefix "\nslug from the memory content — " ;
          oa:suffix " — and commit the file successfu"
        ]
      ] .

    # ── Style Observation 2: Imperative/obligatory mood ───────────
    narr:StyleObs_ImperativeMood a narr:StyleObservation ;
      skos:broader narr:SentenceMood ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "Obligatory modal 'must' used twice to assert pipeline requirements as non-negotiable constraints." ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start "132"^^xsd:integer ;
          oa:end "188"^^xsd:integer
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "The pipeline must produce a\nslug from the memory content" ;
          oa:prefix "d without an explicit id field. " ;
          oa:suffix " — either via the LLM id-gen cal"
        ]
      ] .

    # ── Style Observation 3: Disjunctive fallback framing ─────────
    narr:StyleObs_PassiveConstruction a narr:StyleObservation ;
      skos:broader narr:GrammarSyntax ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "Disjunctive 'either … or' construction used to enumerate two alternative execution paths (LLM id-gen vs. deterministic slugify), a pattern common in technical specification prose." ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start "191"^^xsd:integer ;
          oa:end "259"^^xsd:integer
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "either via the LLM id-gen call or the\ndeterministic slugify fallback" ;
          oa:prefix "\nslug from the memory content — " ;
          oa:suffix " — and commit the file successfu"
        ]
      ] .

    # ── Style Metrics ─────────────────────────────────────────────
    narr:StyleMetrics_SampleMemoryDistinctiveHeading a narr:StyleMetrics ;
      skos:broader narr:StyleMetrics ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      narr:AverageSentenceLength "18"^^xsd:integer ;
      narr:ActiveVoiceRatio "0.75"^^xsd:decimal ;
      narr:JargonDensity "0.20"^^xsd:decimal ;
      skos:note "Short document (2 sentences in body). Active voice dominant; passive construction absent. Moderate jargon density due to technical terms: LLM, id-gen, slugify, pipeline." .

  }
}