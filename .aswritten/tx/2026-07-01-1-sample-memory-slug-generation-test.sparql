PREFIX narr: <https://aswritten.ai/narrative#>
PREFIX rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX dct:  <http://purl.org/dc/terms/>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX oa:   <http://www.w3.org/ns/oa#>

INSERT DATA {
  narr:Tx_20260701T233156Z a prov:Activity ;
    prov:generatedAtTime "2026-07-01T23:31:56Z"^^xsd:dateTime ;
    prov:wasAssociatedWith "aswritten.ai" ;
    prov:wasAttributedTo "n8n.aswritten.ai" ;
    narr:originPath "2026-07-01-1-sample-memory-slug-generation-test" ;
    narr:originRef "e2e-derive-id-1782948603908" ;
    dct:title "Sample Memory With Distinctive Heading — slug derivation test" ;
    prov:generated
      narr:Sample_SampleMemoryDistinctiveHeading,
      narr:Narrative_PipelineSlugDerivation,
      narr:Claim_NoExplicitIdSlugRequired,
      narr:Actor_ExpertReviewer_TestExpert,
      narr:Obs_SlugFallbackBehavior,
      narr:StyleObs_TechnicalRegister,
      narr:StyleObs_ImperativeMood,
      narr:StyleObs_JargonDensityPipeline,
      narr:StyleMetrics_SampleMemoryDistinctiveHeading .
} ;

INSERT DATA {
  GRAPH narr:Tx_20260701T233156Z {

    # ── Sample ──────────────────────────────────────────────────
    narr:Sample_SampleMemoryDistinctiveHeading a narr:Sample ;
      dct:title "Sample Memory With Distinctive Heading" ;
      narr:inputLength 308 ;
      dct:description "E2E test memory submitted without an explicit id field to exercise pipeline slug derivation." ;
      dct:created "2025-07-17"^^xsd:date .

    # ── Narrative ────────────────────────────────────────────────
    narr:Narrative_PipelineSlugDerivation a narr:Narrative ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      narr:theme "Pipeline slug derivation from memory content when no explicit id is provided" ;
      skos:broader narr:Process ;
      skos:note "When a memory is submitted without an explicit id field, the pipeline must derive a slug — either via an LLM id-gen call or a deterministic slugify fallback — and commit the file successfully. This memory is an end-to-end test of that behaviour." .

    # ── Claim ────────────────────────────────────────────────────
    narr:Claim_NoExplicitIdSlugRequired a narr:Claim ;
      narr:aboutNode narr:Narrative_PipelineSlugDerivation ;
      narr:hasConvictionLevel narr:Conviction_Foundation ;
      narr:supports narr:Process ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "The pipeline MUST produce a slug from memory content (LLM id-gen or deterministic slugify fallback) and commit the file when no id field is present." .

    # ── Actor ────────────────────────────────────────────────────
    narr:Actor_ExpertReviewer_TestExpert a narr:Actor ;
      skos:prefLabel "test+expert-gh@aswritten.ai" ;
      skos:note "Reviewer listed in YAML frontmatter of the test memory; role: expert reviewer for e2e pipeline validation." .

    # ── Observation ─────────────────────────────────────────────
    narr:Obs_SlugFallbackBehavior a narr:Observation ;
      skos:broader narr:Process ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "The memory explicitly states both the LLM id-gen call and the deterministic slugify fallback as acceptable slug derivation paths." ;
      oa:hasSelector [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 143 ;
          oa:end 299
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "The pipeline must produce a slug from the memory content — either via the LLM id-gen call or the deterministic slugify fallback — and commit the file successfully." ;
          oa:prefix "Submitted without an explicit id field. " ;
          oa:suffix ""
        ]
      ] .

    # ── Style Observations ───────────────────────────────────────
    narr:StyleObs_TechnicalRegister a narr:StyleObservation ;
      skos:broader narr:RegisterFormality ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "Technical/procedural register throughout; document reads as a system specification or test case description rather than narrative prose." ;
      rdf:value true ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 102 ; oa:end 141
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "Submitted without an explicit id field." ;
          oa:prefix "# Sample Memory With Distinctive Heading\n\n" ;
          oa:suffix " The pipeline must produce"
        ]
      ] .

    narr:StyleObs_ImperativeMood a narr:StyleObservation ;
      skos:broader narr:SentenceMood ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "Imperative/obligatory mood expressed via 'must': the document asserts pipeline requirements as non-negotiable obligations." ;
      rdf:value true ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 143 ;
          oa:end 220
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "The pipeline must produce a slug from the memory content" ;
          oa:prefix "Submitted without an explicit id field. " ;
          oa:suffix " — either via the LLM"
        ]
      ] .

    narr:StyleObs_JargonDensityPipeline a narr:StyleObservation ;
      skos:broader narr:JargonPolicy ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "High technical jargon density for a short document: 'slug', 'LLM id-gen call', 'deterministic slugify fallback', 'pipeline', 'commit' — five domain-specific terms in two sentences." ;
      rdf:value true ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 143 ;
          oa:end 299
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "either via the LLM id-gen call or the deterministic slugify fallback — and commit the file successfully." ;
          oa:prefix "slug from the memory content — " ;
          oa:suffix ""
        ]
      ] .

    # ── Style Metrics ────────────────────────────────────────────
    narr:StyleMetrics_SampleMemoryDistinctiveHeading a narr:StyleMetrics ;
      skos:broader narr:StyleMetrics ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      narr:AverageSentenceLength 28 ;
      narr:ActiveVoiceRatio "0.90"^^xsd:decimal ;
      narr:JargonDensity "0.17"^^xsd:decimal ;
      skos:note "Two-sentence body; active voice dominant; high jargon density relative to document length (5 technical terms / ~30 content words). Register: technical specification / E2E test case." .

  }
}