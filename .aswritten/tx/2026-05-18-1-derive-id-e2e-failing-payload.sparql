PREFIX narr: <https://aswritten.ai/narrative#>
PREFIX rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX dct:  <http://purl.org/dc/terms/>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX oa:   <http://www.w3.org/ns/oa#>

# ── Transaction provenance (default graph) ──────────────────────────────────
INSERT DATA {
  narr:Tx_20250720T120000Z a prov:Activity ;
    prov:generatedAtTime "2025-07-20T12:00:00Z"^^xsd:dateTime ;
    prov:wasAssociatedWith "aswritten.ai" ;
    prov:wasAttributedTo "n8n.aswritten.ai" ;
    narr:originPath ".aswritten/memories/box-free-design-continuation-2-derive-id-e2e.md" ;
    narr:originRef "main@unknown" ;
    dct:title "Box-Free Design Continuation 2 — Derive Id E2E" ;
    prov:generated
      narr:Sample_BoxFreeDesignContinuation2,
      narr:Actor_MikeSackton,
      narr:Narrative_BoxFreeDesignDeriveIdE2E,
      narr:StyleObs_BoxFree_TechnicalRegister,
      narr:StyleObs_BoxFree_ImperativeMood,
      narr:StyleObs_BoxFree_ParentheticalQualifier,
      narr:StyleMetrics_BoxFreeDesign2 .
} ;

# ── Extracted entities (named graph) ────────────────────────────────────────
INSERT DATA {
  GRAPH narr:Tx_20250720T120000Z {

    # ── Sample ──────────────────────────────────────────────────────────────
    narr:Sample_BoxFreeDesignContinuation2 a narr:Sample ;
      dct:title "Box-Free Design Continuation 2 — Derive Id E2E" ;
      dct:description "Second continuation test for the box-free design pipeline. Reproduces Mike Sackton's failing payload pattern (## level heading, no id field). Validates that the pipeline does not crash and produces a topical slug." ;
      narr:inputLength 221 .

    # ── Actor: Mike Sackton ─────────────────────────────────────────────────
    narr:Actor_MikeSackton a narr:Actor ;
      skos:prefLabel "Mike Sackton" ;
      skos:note "Named as the originator of the failing payload pattern (## level heading, no id) that this E2E test reproduces. Role: pipeline stress-tester / edge-case reporter." .

    # ── Narrative ───────────────────────────────────────────────────────────
    narr:Narrative_BoxFreeDesignDeriveIdE2E a narr:Narrative ;
      dct:source narr:Sample_BoxFreeDesignContinuation2 ;
      skos:broader narr:NarrativeTestPrompts ;
      skos:note "A continuation E2E test that exercises the pipeline's id-derivation path when a memory arrives with a ## level heading and no explicit id field. The invariant: the pipeline must not crash and must emit a topical slug. Reproduces Mike Sackton's failing payload pattern." ;
      narr:theme "Pipeline robustness: derive topical slug from heading when id is absent" .

    # ── Style Observations ──────────────────────────────────────────────────

    # SO-1: Technical register — developer-facing jargon
    narr:StyleObs_BoxFree_TechnicalRegister a narr:StyleObservation ;
      skos:broader narr:JargonPolicy ;
      dct:source narr:Sample_BoxFreeDesignContinuation2 ;
      skos:note "Dense developer-register jargon: 'payload pattern', 'id', 'topical slug' in a two-sentence body. Signals an internal engineering artifact, not a user-facing document." ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_BoxFreeDesignContinuation2 ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 89 ;
          oa:end 175
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "failing payload pattern (## level heading,\nno id). Must not crash; must produce a topical slug." ;
          oa:prefix "Reproduces Mike Sackton's " ;
          oa:suffix ""
        ]
      ] .

    # SO-2: Imperative mood — terse engineering requirements
    narr:StyleObs_BoxFree_ImperativeMood a narr:StyleObservation ;
      skos:broader narr:SentenceMood ;
      dct:source narr:Sample_BoxFreeDesignContinuation2 ;
      skos:note "Requirements stated as bare imperatives without subject: 'Must not crash; must produce a topical slug.' Characteristic of internal test-spec writing style." ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_BoxFreeDesignContinuation2 ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 143 ;
          oa:end 175
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "Must not crash; must produce a topical slug." ;
          oa:prefix "no id). " ;
          oa:suffix ""
        ]
      ] .

    # SO-3: Parenthetical qualifier — inline constraint specification
    narr:StyleObs_BoxFree_ParentheticalQualifier a narr:StyleObservation ;
      skos:broader narr:GrammarSyntax ;
      dct:source narr:Sample_BoxFreeDesignContinuation2 ;
      skos:note "Parenthetical used to embed the precise failure condition inline: '(## level heading, no id)'. Compact, code-comment-like style that avoids a separate sentence for constraint detail." ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_BoxFreeDesignContinuation2 ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 113 ;
          oa:end 141
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "(## level heading,\nno id)" ;
          oa:prefix "Mike Sackton's failing payload pattern " ;
          oa:suffix ". Must not crash"
        ]
      ] .

    # ── Style Metrics ───────────────────────────────────────────────────────
    narr:StyleMetrics_BoxFreeDesign2 a narr:StyleMetrics ;
      dct:source narr:Sample_BoxFreeDesignContinuation2 ;
      skos:broader narr:StyleMetrics ;
      skos:note "Two-sentence body (excluding frontmatter and heading). Fully active-voice requirements. High jargon density for document length: payload, id, slug, ## heading marker all appear in 32 words." ;
      narr:AverageSentenceLength "16"^^xsd:integer ;
      narr:ActiveVoiceRatio "1.00"^^xsd:decimal ;
      narr:JargonDensity "0.19"^^xsd:decimal .

  }
} ;

# ── UPDATE: Claim_PipelineSlugGenRequired — add specificity from this memory ─
DELETE {
  GRAPH ?g {
    narr:Claim_PipelineSlugGenRequired skos:note ?oldNote .
  }
}
INSERT {
  GRAPH narr:Tx_20250720T120000Z {
    narr:Claim_PipelineSlugGenRequired skos:note "The pipeline must produce a slug from memory content when no explicit id field is provided — either via the LLM id-gen call or the deterministic slugify fallback — and commit the file successfully. Confirmed edge case: ## level heading with no id (Mike Sackton's failing payload pattern) must not crash and must emit a topical slug (Box-Free Design Continuation 2 E2E test)." .
  }
}
WHERE {
  GRAPH ?g {
    narr:Claim_PipelineSlugGenRequired skos:note ?oldNote .
  }
}