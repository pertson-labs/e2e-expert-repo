PREFIX narr: <https://aswritten.ai/narrative#>
PREFIX rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX dct:  <http://purl.org/dc/terms/>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX oa:   <http://www.w3.org/ns/oa#>

# Transaction provenance (default graph)
INSERT DATA {
  narr:Tx_20250115T120000Z a prov:Activity ;
    prov:generatedAtTime "2025-01-15T12:00:00Z"^^xsd:dateTime ;
    prov:wasAssociatedWith "aswritten.ai" ;
    prov:wasAttributedTo "n8n.aswritten.ai" ;
    narr:originPath "sample-memory-with-distinctive-heading" ;
    narr:originRef "main" ;
    dct:title "Sample Memory With Distinctive Heading — Pipeline Slug Test" ;
    prov:generated
      narr:Sample_SampleMemoryDistinctiveHeading,
      narr:Narrative_PipelineSlugGeneration,
      narr:Claim_SlugFromContent,
      narr:Actor_TestExpertReviewer,
      narr:Observation_LLMSlugGenOrFallback,
      narr:Observation_CommitSuccessRequirement,
      narr:StyleObs_TechnicalRegister,
      narr:StyleObs_ImperativeMood,
      narr:StyleObs_PassiveConstruction,
      narr:StyleMetrics_SampleMemoryDistinctiveHeading .
} ;

# Extracted entities (named graph)
INSERT DATA {
  GRAPH narr:Tx_20250115T120000Z {

    # ── Sample ──────────────────────────────────────────────────
    narr:Sample_SampleMemoryDistinctiveHeading a narr:Sample ;
      dct:title "Sample Memory With Distinctive Heading" ;
      dct:created "2025-01-15"^^xsd:date ;
      narr:inputLength 342 ;
      skos:note "Test memory submitted without an explicit id field to exercise the pipeline slug-generation path (LLM id-gen or deterministic slugify fallback)." .

    # ── Actor ───────────────────────────────────────────────────
    narr:Actor_TestExpertReviewer a narr:Actor ;
      skos:prefLabel "test+expert-gh@aswritten.ai" ;
      skos:note "Reviewer listed in frontmatter of the test memory. Email address used as identifier per frontmatter declaration." .

    # ── Narrative ───────────────────────────────────────────────
    narr:Narrative_PipelineSlugGeneration a narr:Narrative ;
      narr:theme "Pipeline slug generation from memory content when no explicit id is provided" ;
      skos:broader narr:Process ;
      skos:note "When a memory is submitted without an explicit id field, the pipeline must derive a slug from the memory content — either via an LLM id-gen call or a deterministic slugify fallback — and commit the file successfully. This memory exists specifically to exercise and validate that path." ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading .

    # ── Claims ──────────────────────────────────────────────────
    narr:Claim_SlugFromContent a narr:Claim ;
      narr:aboutNode narr:Narrative_PipelineSlugGeneration ;
      narr:hasConvictionLevel narr:Conviction_Stake ;
      narr:supports narr:Process ;
      skos:note "The pipeline must produce a slug from the memory content when no id field is present, using either the LLM id-gen call or the deterministic slugify fallback." ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading .

    # ── Observations ────────────────────────────────────────────
    narr:Observation_LLMSlugGenOrFallback a narr:Observation ;
      skos:broader narr:CoreWorkflows ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "Pipeline must produce a slug via LLM id-gen call or deterministic slugify fallback" ;
      oa:hasSelector [
        a oa:TextPositionSelector ;
        oa:start 175 ;
        oa:end 310
      ] ;
      oa:hasSelector [
        a oa:TextQuoteSelector ;
        oa:exact "either via the LLM id-gen call or the\ndeterministic slugify fallback" ;
        oa:prefix "slug from the memory content — " ;
        oa:suffix " — and commit the file successfully."
      ] .

    narr:Observation_CommitSuccessRequirement a narr:Observation ;
      skos:broader narr:CoreWorkflows ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "The pipeline must commit the file successfully after slug generation" ;
      oa:hasSelector [
        a oa:TextPositionSelector ;
        oa:start 310 ;
        oa:end 342
      ] ;
      oa:hasSelector [
        a oa:TextQuoteSelector ;
        oa:exact "and commit the file successfully." ;
        oa:prefix "deterministic slugify fallback — " ;
        oa:suffix ""
      ] .

    # ── Style Observations ──────────────────────────────────────
    narr:StyleObs_TechnicalRegister a narr:StyleObservation ;
      skos:broader narr:RegisterFormality ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "Technical/engineering register throughout; uses pipeline-specific vocabulary (slug, id-gen, slugify fallback, commit) without definition, assuming a developer audience." ;
      rdf:value true ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 130 ;
          oa:end 230
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "The pipeline must produce a\nslug from the memory content" ;
          oa:prefix "Submitted without an explicit id field. " ;
          oa:suffix " — either via the LLM id-gen call"
        ]
      ] .

    narr:StyleObs_ImperativeMood a narr:StyleObservation ;
      skos:broader narr:SentenceMood ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "Imperative/prescriptive mood: 'must produce', 'must … commit' — characteristic of test-specification or acceptance-criteria writing." ;
      rdf:value true ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 130 ;
          oa:end 175
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "The pipeline must produce a" ;
          oa:prefix "Submitted without an explicit id field. " ;
          oa:suffix "\nslug from the memory content"
        ]
      ] .

    narr:StyleObs_PassiveConstruction a narr:StyleObservation ;
      skos:broader narr:VoicePassive ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      skos:note "Opening sentence uses passive construction ('Submitted without an explicit id field') — the agent (submitter) is elided, foregrounding the artifact state rather than the actor." ;
      rdf:value true ;
      oa:hasTarget [
        a oa:SpecificResource ;
        oa:hasSource narr:Sample_SampleMemoryDistinctiveHeading ;
        oa:hasSelector [
          a oa:TextPositionSelector ;
          oa:start 90 ;
          oa:end 130
        ] ;
        oa:hasSelector [
          a oa:TextQuoteSelector ;
          oa:exact "Submitted without an explicit id field." ;
          oa:prefix "# Sample Memory With Distinctive Heading\n\n" ;
          oa:suffix " The pipeline must produce a"
        ]
      ] .

    # ── Style Metrics ───────────────────────────────────────────
    narr:StyleMetrics_SampleMemoryDistinctiveHeading a narr:StyleMetrics ;
      skos:broader narr:StyleMetrics ;
      dct:source narr:Sample_SampleMemoryDistinctiveHeading ;
      narr:AverageSentenceLength 22 ;
      narr:ActiveVoiceRatio "0.50"^^xsd:decimal ;
      narr:JargonDensity "0.18"^^xsd:decimal ;
      skos:note "Short document (2 sentences in body). Mixed active/passive voice. Moderate jargon density driven by pipeline-specific terms (slug, id-gen, slugify, fallback, commit). Formal-technical register." .

  }
}